"""Contract tests — verify every adapter honours the same `BaseSender` API.

When W6 adds DiscordSender / MessengerSender, those go through the same
parametrised tests here. No platform-specific code lives in this file."""
