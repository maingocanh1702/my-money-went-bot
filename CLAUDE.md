# Claude Setup Notes

Read `docs/AI_SETUP.md` before guiding a user through setup.

The simplest user path is Railway deployment. Help the user collect the seven required env vars, deploy the repo, set Telegram webhook, set SePay webhook, then test `/today`.

Do not ask the user to paste real secrets into public chat or commit `.env` / `credentials.json`.
