# Test and development dependencies.
#   pip install -r requirements-dev.txt
# Pulls in the runtime set too, so one command gives a working test environment.
-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
freezegun==1.5.5

# storage/ (the PostgreSQL source-of-truth work) is not imported by the running
# bot yet, so its drivers stay out of the deploy image until the cutover lands.
SQLAlchemy==2.0.52
psycopg[binary]==3.3.5

# The only lint CI enforces (E9,F63,F7,F82). Pinned so a local run and the
# CI run agree; CONTRIBUTING tells contributors to install this file.
ruff==0.15.14
